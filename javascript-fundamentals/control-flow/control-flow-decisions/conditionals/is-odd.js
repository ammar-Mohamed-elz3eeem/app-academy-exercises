/**
 * This functions returns true if a number is odd
 * and false if it is not.
 *
 * @param {number} num
 *     This is the number to check.
 *
 * @returns
 *     true if the given num is odd, otherwise false.
 */
function isOdd(num) {
	return num % 2 !== 0;
}
