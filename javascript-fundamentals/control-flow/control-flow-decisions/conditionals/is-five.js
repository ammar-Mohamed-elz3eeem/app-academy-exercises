/**
 * This functions returns true if a number is equal
 * to 5 and false if it is not.
 * 
 * @param {number} num
 *     This is the number to check.
 * 
 * @returns
 *     true if the given num is 5, otherwise false.
 */
function isFive (num) {
  return num === 5;
}